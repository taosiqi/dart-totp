name: dart_totp
description: 从密钥生成TOTP验证码 Generate TOTP tokens from key
version: 1.0.0
repository: https://github.com/taosiqi/dart-totp

environment:
  sdk: ^3.0.6

# Add regular dependencies here.
dependencies:
  base32: ^2.1.3
  crypto: ^3.0.3

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
